'use server';

export default async function TermsAndConditionsPage() {
    return (
        <main className="min-h-screen w-full flex items-center justify-center">
            <h2 className="h2">Booking terms page in development</h2>
        </main>
    );
}
