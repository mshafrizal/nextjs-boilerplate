'use server';

export default async function RoomsPage() {
    return (
        <main className={'min-h-screen w-full'}>
            <h2 className="h2">Rooms page in development</h2>
        </main>
    );
}
