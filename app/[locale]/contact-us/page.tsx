'use server';

export default async function ContactUsPage() {
    return (
        <main className="min-h-screen w-full flex items-center justify-center">
            <h2 className="h2">Contact Us Page in development</h2>
        </main>
    );
}
