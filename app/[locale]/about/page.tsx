'use server';

export default async function AboutPage() {
    return (
        <main className={'min-h-screen w-full'}>
            <h2 className="h2">About page in development</h2>
        </main>
    );
}
