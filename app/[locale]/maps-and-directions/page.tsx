'use server';

export default async function MapsAndDirectionsPage() {
    return (
        <main className={'min-h-screen w-full'}>
            <h2 className="h2">Maps & Directions page in development</h2>
        </main>
    );
}
