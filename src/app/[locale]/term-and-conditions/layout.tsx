import MainPage from "@/components/layout/main/page"
import Link from "next/link"

export default function TnCLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return children
  }